---
import { buildClearAdminSessionCookie } from '../../lib/adminAuth';
import { routes } from '../../lib/routes';

export const prerender = false;

return new Response(null, {
  status: 303,
  headers: {
    Location: routes.adminLogin,
    'Set-Cookie': buildClearAdminSessionCookie(Astro),
  },
});
---
